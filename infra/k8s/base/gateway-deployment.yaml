apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: slsa-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: ghcr.io/example/gateway:latest
          ports:
            - containerPort: 8080
